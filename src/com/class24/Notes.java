package com.class24;

public class Notes {

	/*When overloading we only mean method signature(name of method and signature)
	 * method overloading is not possible by changing the return type of the method
	 * key points; - Methods have the same name within a class
	 *             - Method signature must be different
	 *             - Overload possible by changing:type of parameters
	 *                                             number of parameters
	 *                                             
	 *  Method overriding
	 *  ..same method in 2 different class(Parent-Child)
	 *  
	 *  Without inheritance method overriding is not possible Runtime polymorphism
	 *  
	 *  in Method Overriding - Method Signature must be the same (parameters and 
	 */
}
